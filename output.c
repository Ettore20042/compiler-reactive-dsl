#include <stdio.h>
#include <stdbool.h>

int main() {
    int speed = 0;
    int max_speed = 100;
    bool running = true;
    if ((speed < max_speed)) {
        speed = (speed + 10);
    }
    while ((running == true)) {
        running = false;
    }
    return 0;
}
